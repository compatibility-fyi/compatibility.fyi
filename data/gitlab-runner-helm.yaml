x-gitlab-runner-helm-index-source: &gitlab_runner_helm_index_source
  title: GitLab Helm chart repository index
  url: https://charts.gitlab.io/index.yaml
  accessedAt: '2026-08-25'

x-gitlab-runner-compatibility-source: &gitlab_runner_compatibility_source
  title: GitLab Runner version compatibility
  url: https://docs.gitlab.com/runner/
  accessedAt: '2026-07-22'

x-gitlab-helm-version-mapping-source: &gitlab_helm_version_mapping_source
  title: GitLab Helm chart version mappings
  url: https://docs.gitlab.com/charts/installation/version_mappings/
  accessedAt: '2026-07-22'

projects:
  gitlab-runner-helm:
    name: GitLab Runner Helm Chart
    categories:
      - CI/CD
      - Automation
      - Kubernetes
      - GitLab
    website: https://docs.gitlab.com/runner/install/kubernetes/
    description: GitLab Runner Helm chart compatibility with GitLab Helm chart versions.
    versions:
      '0.92.1':
        dependencies:
          gitlab-helm:
            ranges:
              - '>=10.3.0 <10.4.0'
            relationship: coordinator chart
            confidence: high
            notes:
              - GitLab Runner chart 0.92.1 maps to GitLab Runner 19.3.1, while GitLab chart 10.3.x maps to GitLab 19.3.x.
              - GitLab recommends keeping GitLab Runner major.minor synchronized with the GitLab major.minor version.
            sources:
              - *gitlab_runner_helm_index_source
              - *gitlab_runner_compatibility_source
              - *gitlab_helm_version_mapping_source
            lastVerified: '2026-08-25'
      '0.91.3':
        dependencies:
          gitlab-helm:
            ranges:
              - '>=10.2.0 <10.3.0'
            relationship: coordinator chart
            confidence: high
            notes:
              - GitLab Runner chart 0.91.3 maps to GitLab Runner 19.2.3, while GitLab chart 10.2.x maps to GitLab 19.2.x.
              - GitLab recommends keeping GitLab Runner major.minor synchronized with the GitLab major.minor version.
            sources:
              - *gitlab_runner_helm_index_source
              - *gitlab_runner_compatibility_source
              - *gitlab_helm_version_mapping_source
            lastVerified: '2026-08-25'
      '0.90.2':
        dependencies:
          gitlab-helm:
            ranges:
              - '>=10.1.0 <10.2.0'
            relationship: coordinator chart
            confidence: high
            notes:
              - GitLab Runner chart 0.90.2 maps to GitLab Runner 19.1.2, while GitLab chart 10.1.x maps to GitLab 19.1.x.
              - GitLab recommends keeping GitLab Runner major.minor synchronized with the GitLab major.minor version.
            sources:
              - *gitlab_runner_helm_index_source
              - *gitlab_runner_compatibility_source
              - *gitlab_helm_version_mapping_source
            lastVerified: '2026-07-31'
      '0.89.3':
        dependencies:
          gitlab-helm:
            ranges:
              - '>=10.0.0 <10.1.0'
            relationship: coordinator chart
            confidence: high
            notes:
              - GitLab Runner chart 0.89.3 maps to GitLab Runner 19.0.3, while GitLab chart 10.0.x maps to GitLab 19.0.x.
              - GitLab recommends keeping GitLab Runner major.minor synchronized with the GitLab major.minor version.
            sources:
              - *gitlab_runner_helm_index_source
              - *gitlab_runner_compatibility_source
              - *gitlab_helm_version_mapping_source
            lastVerified: '2026-07-31'
      '0.88.4':
        dependencies:
          gitlab-helm:
            ranges:
              - '>=9.11.0 <9.12.0'
            relationship: coordinator chart
            confidence: high
            notes:
              - GitLab Runner chart 0.88.4 maps to GitLab Runner 18.11.4, while GitLab chart 9.11.x maps to GitLab 18.11.x.
              - GitLab recommends keeping GitLab Runner major.minor synchronized with the GitLab major.minor version.
            sources:
              - *gitlab_runner_helm_index_source
              - *gitlab_runner_compatibility_source
              - *gitlab_helm_version_mapping_source
            lastVerified: '2026-07-22'
